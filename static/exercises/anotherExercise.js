describe("", function () {
    it("Create the variables 'gerd' and 'renke10' and initialize them", function () {
        expect(typeof window.gerd).not.toBe("undefined");
        expect(typeof window.renke10).not.toBe("undefined");
    });
});
